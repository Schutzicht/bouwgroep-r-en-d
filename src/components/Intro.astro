---
---

<section id="about" class="section">
  <div class="container">
    <div class="intro-grid">
      <div class="intro-text">
        <h4 class="text-primary">Over R&D Bouwgroep</h4>
        <h2>Waar Luxe en Comfort <br>Samenkomen</h2>
        <p class="lead">
          Uw keus voor exclusief sanitair, tegelwerken en wellness.
        </p>
        <p>
          R&D Bouwgroep is uw specialist voor renovaties en nieuwbouw in het hogere segment. 
          Of het nu gaat om een complete badkamerverbouwing, het leggen van een exclusieve marmeren vloer 
          of het realiseren van uw eigen wellness aan huis: wij leveren vakwerk.
        </p>
        <div class="features">
          <div class="feature">
            <span class="check">✓</span>
            <span>Persoonlijke begeleiding</span>
          </div>
          <div class="feature">
            <span class="check">✓</span>
            <span>Hoogwaardige materialen</span>
          </div>
          <div class="feature">
            <span class="check">✓</span>
            <span>Jarenlange ervaring</span>
          </div>
        </div>
        <a href="#contact" class="btn" style="margin-top: 2rem;">Meer over ons</a>
      </div>

      <div class="intro-images">
        <div class="img-1">
          <img src="https://www.rd-bouwgroep.nl/wp-content/uploads/2024/06/badkamer.jpg" alt="Moderne Badkamer" />
        </div>
        <div class="img-2">
          <img src="https://www.rd-bouwgroep.nl/wp-content/uploads/2024/06/zwart-marmer.jpg" alt="Marmer Detail" />
          <div class="exp-box">
            <span class="num">10+</span>
            <span class="txt">Jaar<br>Ervaring</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .intro-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  h4 {
    font-family: var(--font-body);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 0.9rem;
    margin-bottom: 1rem;
  }

  h2 {
    margin-bottom: 1.5rem;
    color: var(--color-dark);
  }

  .lead {
    font-size: 1.25rem;
    color: var(--color-dark);
    font-weight: 600;
    margin-bottom: 1rem;
  }

  p {
    color: var(--color-text-muted);
    margin-bottom: 2rem;
  }

  .features {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .feature {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-weight: 500;
    color: var(--color-dark);
  }

  .check {
    color: var(--color-primary);
    font-weight: 800;
  }

  .intro-images {
    position: relative;
    height: 600px;
  }

  .img-1 {
    width: 70%;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 1;
    border-radius: 4px;
    overflow: hidden;
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
  }

  .img-2 {
    width: 60%;
    position: absolute;
    bottom: 0;
    right: 0;
    z-index: 2;
    border: 8px solid white;
    border-radius: 4px;
    overflow: visible; /* Allow box to stick out */
    box-shadow: 0 20px 40px rgba(0,0,0,0.15);
  }
  
  .img-2 img {
    width: 100%;
  }

  .exp-box {
    position: absolute;
    bottom: 20px;
    left: -40px;
    background-color: var(--color-primary);
    color: white;
    padding: 1.5rem;
    text-align: center;
    box-shadow: 0 10px 20px rgba(160, 10, 19, 0.3);
  }

  .num {
    display: block;
    font-size: 2.5rem;
    font-weight: 800;
    line-height: 1;
  }

  .txt {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  @media (max-width: 900px) {
    .intro-grid { grid-template-columns: 1fr; }
    .intro-images { height: 400px; margin-top: 3rem; }
  }
</style>
